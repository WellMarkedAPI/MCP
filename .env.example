# Your WellMarked API key. Generate one at https://wellmarked.io.
WELLMARKED_API_KEY=wm_xxxxxxxxxxxxxxxxxxxxxxxx

# Optional: override the API base URL (e.g. for a self-hosted instance).
# WELLMARKED_BASE_URL=https://api.wellmarked.io

# Optional: per-request timeout in milliseconds (default 30000).
# WELLMARKED_TIMEOUT_MS=30000
